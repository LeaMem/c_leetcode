{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "LeaMem",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "${file}",
                "${fileDirname}/base/common.c",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "group": "build"
        }
    ]
}