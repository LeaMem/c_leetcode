{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "LeaMem",
            "command": "/usr/local/bin/gcc-8",
            "args": [
                "-g",
                "${file}",
                "${fileDirname}/base/common.c",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/local/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        },
        {
            "type": "shell",
            "label": "gcc-8 build active file",
            "command": "/usr/local/bin/gcc-8",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/local/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}